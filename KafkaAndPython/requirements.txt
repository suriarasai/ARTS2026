# requirements.txt — install with: pip install -r requirements.txt
#
# All packages below ship pre-built wheels for Python 3.12, 3.13, and 3.14
# on Windows, macOS, and Linux — no C compiler required.

# Kafka client (wraps librdkafka; includes Avro/Schema Registry support)
confluent-kafka[schemaregistry,avro]==2.14.0

# Data validation and serialisation
pydantic==2.11.4

# .env file loading
python-dotenv==1.1.0
